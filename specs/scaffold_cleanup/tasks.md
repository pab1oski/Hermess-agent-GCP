# Tasks — scaffold_cleanup

- [x] T1 — Borrar `src/`, `tests/`, `specs/example_feature/` con `rm -rf`. Cubre: R1.
- [x] T2 — Reescribir `.gitignore` con reglas Terraform, secretos, Python, OS. Cubre: R5.
- [x] T3 — Reescribir `feature_list.json` con las 10 features reales. Cubre: R3.
- [x] T4 — Crear `specs/<feature>/requirements.md`, `design.md`, `tasks.md` para las 10 features. Cubre: R4.
- [x] T5 — Crear estructura de carpetas `terraform/`, `scripts/`, `config/`, `skills/`, `docs/cursor-ssh-guide.md`. Cubre: R2.
- [x] T6 — Verificar con `./init.sh` que todo pasa. Cubre: R6.
