# Unique name for this agent instance (used in branch names and PR attribution)
AGENT_NAME=hermes

# Master key for the LiteLLM proxy (generated during provision)
LITELLM_MASTER_KEY=

# GitHub Personal Access Token with repo + issues + pull_requests scopes
GITHUB_TOKEN=

# GCP Project ID where Vertex AI is enabled
GOOGLE_CLOUD_PROJECT=
